import 'package:flutter/material.dart';

class DoorServiceForum extends StatelessWidget {
  const DoorServiceForum({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: const Text('data'),
      ),
    );
  }
}
