import 'package:flutter/material.dart';

class WindowForum extends StatelessWidget {
  const WindowForum({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('data'));
  }
}
